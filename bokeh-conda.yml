# conda env create -f bokeh-conda.yml
# or conda env update --name myenv --file local.yml --prune
# or conda activate myenv
#    and conda env update -f environment.yml --prune

# (base) conda install ipykernel
# (base) python -m ipykernel --user --name bokeh --display-name "Python (bokeh)"

# github EBISYS / WaterWatch / database.csv
# git clone https://github.com/bokeh/bokeh-notebooks.git 6_Bokeh

name: bokeh
dependencies:
  - bokeh
  - django
  - findspark
  - ipywidgets
  - jupyterlab
  - nodejs
  - pandas
  - pymongo
  - python
  - sparkmagic==0.13.1
  - pyspark==2.4.8